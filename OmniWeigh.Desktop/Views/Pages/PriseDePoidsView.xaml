<UserControl x:Class="OmniWeigh.Desktop.Views.Pages.PriseDePoidsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="950">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- Dropdown item template for Products -->
        <DataTemplate x:Key="ProductItemTemplate">
            <StackPanel Orientation="Horizontal">
                <!-- Image Container with Fallback -->
                <Grid Height="30" MinWidth="30" Margin="0,0,10,0">
                    <TextBlock Text="📦" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" x:Name="FallbackIcon"/>
                    <Image Source="{Binding ImagePath}" Stretch="Uniform" Height="30" x:Name="ItemImage"/>
                </Grid>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="Black" FontWeight="SemiBold"/>
            </StackPanel>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                    <Setter TargetName="ItemImage" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FallbackIcon" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ImagePath}" Value="">
                    <Setter TargetName="ItemImage" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FallbackIcon" Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
        
        <!-- Dropdown item template for Vehicles -->
        <DataTemplate x:Key="VehicleItemTemplate">
            <StackPanel Orientation="Horizontal">
                <!-- Image Container with Fallback -->
                <Grid Height="30" MinWidth="30" Margin="0,0,10,0">
                    <TextBlock Text="🚛" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" x:Name="FallbackIcon"/>
                    <Image Source="{Binding ImagePath}" Stretch="Uniform" Height="30" x:Name="ItemImage"/>
                </Grid>
                <TextBlock Text="{Binding Registration}" VerticalAlignment="Center" Foreground="Black" FontWeight="SemiBold"/>
            </StackPanel>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                    <Setter TargetName="ItemImage" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FallbackIcon" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ImagePath}" Value="">
                    <Setter TargetName="ItemImage" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FallbackIcon" Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="240"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR: SESSION INFO -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔴 SESSION EN COURS" Foreground="{StaticResource AccentWarningBrush}" FontSize="14" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,15,10,0">
                    <TextBlock Text="Type de document" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding DocumentTypes}" SelectedItem="{Binding DocumentType}" Height="32"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,15,10,0">
                    <TextBlock Text="Date" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <DatePicker Height="32" BorderThickness="1" BorderBrush="{StaticResource FieldBorderBrush}" Background="{StaticResource FieldBackgroundBrush}" Foreground="Black"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="10,15,10,0">
                    <TextBlock Text="Numéro" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding DocumentNumber}" IsReadOnly="True" Height="32" Foreground="{StaticResource MutedSecondaryBrush}" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="3" Margin="10,15,10,0">
                    <TextBlock Text="Opérateur" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding OperatorName}" Height="32"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="4" Margin="10,15,0,0">
                    <TextBlock Text="Heure" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <Border Background="#1A2A44" CornerRadius="4" Height="32">
                        <TextBlock Text="{Binding CurrentTime, Mode=OneWay}" Foreground="#00FFCC" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Consolas"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MAIN WEIGHT DISPLAY -->
        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="25,15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="POIDS ACTUEL (BRUT)" Foreground="{StaticResource AccentInfoBrush}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding CurrentWeight, StringFormat='{}{0:F2}'}" Foreground="White" FontSize="96" FontWeight="Bold" VerticalAlignment="Bottom"/>
                        <TextBlock Text=" kg" Foreground="{StaticResource AccentInfoBrush}" FontSize="48" FontWeight="Bold" VerticalAlignment="Bottom" Margin="10,0,0,15"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Ellipse Width="10" Height="10" Fill="{StaticResource AccentSuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding StabilityStatus}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Background="{StaticResource FieldBackgroundBrush}" CornerRadius="8" Margin="10" Padding="15" BorderBrush="{StaticResource FieldBorderBrush}" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="RÉFÉRENCE PESÉE" Foreground="{StaticResource MutedTextBrush}" FontSize="11" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding WeighingReference}" Foreground="White" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        
                        <TextBlock Text="ENTRÉES ENREGISTRÉES" Foreground="{StaticResource MutedTextBrush}" FontSize="11" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding SessionEntries.Count}" Foreground="{StaticResource AccentWarningBrush}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- BOTTOM DETAILS & ACTIONS -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <!-- INFOS -->
            <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="DÉTAILS DE LA PESÉE" Foreground="#3498DB" FontSize="12" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="45"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Client" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" SelectedItem="{Binding SelectedClientItem}" ItemsSource="{Binding ClientsList}" DisplayMemberPath="Name" Height="35"/>

                        <TextBlock Grid.Row="1" Text="Produit" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" SelectedItem="{Binding SelectedProductItem}" ItemsSource="{Binding ProductsList}" ItemTemplate="{StaticResource ProductItemTemplate}" Height="35"/>

                        <TextBlock Grid.Row="2" Text="Quantité" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Quantity}" Height="35" Foreground="White" Background="{StaticResource FieldBackgroundBrush}" BorderBrush="{StaticResource FieldBorderBrush}"/>

                        <TextBlock Grid.Row="3" Text="Unité" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="3" Grid.Column="1" SelectedItem="{Binding SelectedUnit}" ItemsSource="{Binding Units}" Height="35"/>

                        <TextBlock Grid.Row="4" Text="Véhicule" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="4" Grid.Column="1" SelectedItem="{Binding SelectedVehicleItem}" ItemsSource="{Binding VehiclesList}" ItemTemplate="{StaticResource VehicleItemTemplate}" Height="35"/>
                        
                        <TextBlock Grid.Row="5" Text="Observation" Foreground="{StaticResource MenuForegroundBrush}" Margin="0,10,0,0"/>
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Observation}" Height="50" Margin="0,10,0,0" Foreground="White" Background="{StaticResource FieldBackgroundBrush}" BorderBrush="{StaticResource FieldBorderBrush}" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- STATS -->
            <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="Poids Brut" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PoidsBrut, StringFormat='{}{0:F2}'}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="1" Text="Tare" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Tare, StringFormat='{}{0:F2}'}" Foreground="{StaticResource AccentWarningBrush}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="2" Text="Poids Net" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PoidsNet, StringFormat='{}{0:F2}'}" Foreground="{StaticResource AccentInfoBrush}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="3" Text="Unité Sélectionnée" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedUnit}" Foreground="White" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="4" Text="Stabilité" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding StabilityStatus}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- ACTIONS -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="💾 Enregistrer la ligne  &gt;" Command="{Binding EnregistrerCommand}" Background="{StaticResource SaveButtonBrush}" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                
                <Button Grid.Row="1" Content="✅ Terminer la série" Command="{Binding TerminerSerieCommand}" Visibility="{Binding HasSessionEntries, Converter={StaticResource BoolToVis}}" Background="{StaticResource AccentWarningBrush}" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
            </Grid>
        </Grid>

        <!-- FOOTER -->
        <Grid Grid.Row="3" VerticalAlignment="Bottom" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Ellipse Width="8" Height="8" Fill="{StaticResource AccentSuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Balance connectée et opérationnelle" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,0,20,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding FrameCount, StringFormat='📈 Données reçues : {0} trames'}" Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
