<UserControl x:Class="OmniWeigh.Desktop.Views.Pages.PriseDePoidsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="280"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="PRISE DE POIDS" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,12,0,0">
                    <TextBlock Text="Type de document" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,0,0,5"/>
                    <RadioButton Content="Bon Livraison" IsChecked="{Binding IsDeliveryNote}" Foreground="White" Margin="0,3"/>
                    <!--<RadioButton Content="Facture" IsChecked="{Binding IsDeliveryNote, Converter={StaticResource InverseBooleanConverter}}" Foreground="White" Margin="0,3"/>-->
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,12,0,0">
                    <TextBlock Text="Date" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <DatePicker Height="32" BorderThickness="1" BorderBrush="{StaticResource FieldBorderBrush}" Background="{StaticResource FieldBackgroundBrush}" Foreground="Black"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="10,12,0,0">
                    <TextBlock Text="Numéro" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding DocumentNumber}" Height="32"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="3" Margin="10,12,0,0">
                    <TextBlock Text="Opérateur" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <ComboBox SelectedItem="{Binding OperatorName}" ItemsSource="{Binding Clients}" Height="32"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="4" Margin="10,12,0,0">
                    <TextBlock Text="Heure" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                    <TextBox Text="10:24:36" IsReadOnly="True" Height="32" Foreground="#3498DB" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="25,15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="280"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="POIDS ACTUEL" Foreground="{StaticResource AccentInfoBrush}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding CurrentWeight, StringFormat='{}{0:F2}'}" Foreground="White" FontSize="96" FontWeight="Bold" VerticalAlignment="Bottom"/>
                        <TextBlock Text=" kg" Foreground="{StaticResource AccentInfoBrush}" FontSize="48" FontWeight="Bold" VerticalAlignment="Bottom" Margin="10,0,0,15"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Ellipse Width="10" Height="10" Fill="{StaticResource AccentSuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding StabilityStatus}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Background="{StaticResource FieldBackgroundBrush}" CornerRadius="8" Margin="10" Padding="15" BorderBrush="{StaticResource FieldBorderBrush}" BorderThickness="1">
                    <Grid>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="INDICATEUR PHYSIQUE" Foreground="{StaticResource MutedTextBrush}" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentWeight, StringFormat='{}{0:F2} kg'}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="36" FontFamily="Consolas" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10"/>
                            <TextBlock Text="[ STABLE ]" Foreground="{StaticResource AccentSuccessBrush}" FontSize="12" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="INFORMATIONS SUPPLÉMENTAIRES" Foreground="#3498DB" FontSize="12" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="42"/>
                            <RowDefinition Height="42"/>
                            <RowDefinition Height="42"/>
                            <RowDefinition Height="42"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Client" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" SelectedItem="{Binding SelectedClient}" ItemsSource="{Binding Clients}" Height="32"/>

                        <TextBlock Grid.Row="1" Text="Produit" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" SelectedItem="{Binding SelectedProduct}" ItemsSource="{Binding Produits}" Height="32"/>

                        <TextBlock Grid.Row="2" Text="Référence" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Reference}" Height="32"/>

                        <TextBlock Grid.Row="3" Text="Véhicule" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="3" Grid.Column="1" SelectedItem="{Binding VehiclePlate}" ItemsSource="{Binding Vehicules}" Height="32"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="38"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="Poids Brut" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PoidsBrut, StringFormat='{}{0:F2} kg'}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="1" Text="Tare" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Tare, StringFormat='{}{0:F2} kg'}" Foreground="{StaticResource AccentWarningBrush}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="2" Text="Poids Net" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PoidsNet, StringFormat='{}{0:F2} kg'}" Foreground="{StaticResource AccentInfoBrush}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="3" Text="Unité" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="kg" Foreground="White" FontSize="14" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="4" Text="État" Foreground="{StaticResource MenuForegroundBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding StabilityStatus}" Foreground="{StaticResource AccentSuccessBrush}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="💾 Enregistrer  &gt;" Command="{Binding EnregistrerCommand}" Background="{StaticResource SaveButtonBrush}" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                <Button Grid.Row="1" Content="🖨️ Imprimer  &gt;" Command="{Binding ImprimerCommand}" Background="{StaticResource PrintButtonBrush}" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                <Button Grid.Row="2" Content="🔄 Remise à zéro  &gt;" Command="{Binding RemiseAZeroCommand}" Background="{StaticResource ResetButtonBrush}" Foreground="White" FontSize="14" FontWeight="SemiBold" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="3" VerticalAlignment="Bottom" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Ellipse Width="8" Height="8" Fill="{StaticResource AccentSuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Balance connectée et opérationnelle" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,0,20,0"/>
                <TextBlock Text="v1.0.0" Foreground="{StaticResource VersionTextBrush}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding FrameCount, StringFormat='📈 Données reçues : {0} trames'}" Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
