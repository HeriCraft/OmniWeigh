<UserControl x:Class="OmniWeigh.Desktop.Views.Pages.ProduitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Produits" Foreground="White" FontSize="20" FontWeight="Bold" Margin="4" />

                <Grid Grid.Row="1" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="160" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <!-- placeholder for future search -->
                    </StackPanel>

                    <Button Grid.Column="1" Content="Nouveau produit" Width="140" Height="32" HorizontalAlignment="Right" Background="{StaticResource SaveButtonBrush}" Foreground="White" FontWeight="SemiBold" Command="{Binding OpenNewProductCommand}" />
                </Grid>

                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding ProductsList}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          RowBackground="{StaticResource FieldBackgroundBrush}"
                          AlternatingRowBackground="{StaticResource CardBackgroundBrush}"
                          GridLinesVisibility="None"
                          Foreground="White"
                          Margin="0,12,0,0"
                          Background="Transparent"
                          ColumnHeaderHeight="38"
                          RowHeight="100">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource FieldBackgroundBrush}" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Padding" Value="8,6" />
                            <Setter Property="BorderBrush" Value="{StaticResource FieldBorderBrush}" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Référence" Binding="{Binding Reference}" Width="200" />
                        <DataGridTextColumn Header="Nom du produit" Binding="{Binding Name}" Width="*" />
                        <DataGridTemplateColumn Header="Image" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="Transparent" Padding="4">
                                        <Image Source="{Binding ImagePath}" Stretch="Uniform" MaxHeight="86" HorizontalAlignment="Center" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
