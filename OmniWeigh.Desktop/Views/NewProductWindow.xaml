<Window x:Class="OmniWeigh.Desktop.Views.NewProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nouveau produit" Height="320" Width="520" WindowStartupLocation="CenterOwner"
        Style="{DynamicResource DialogWindowStyle}">
    <Grid Margin="12">
        <Border Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Ajouter un produit" Foreground="White" FontSize="16" FontWeight="Bold" />

                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="180" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36" />
                        <RowDefinition Height="36" />
                        <RowDefinition Height="36" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom du produit *" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Height="28" Background="{StaticResource FieldBackgroundBrush}" BorderBrush="{StaticResource FieldBorderBrush}" Foreground="White" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Prix unitaire (MGA) *" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" />
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding UnitPrice, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Height="28" Background="{StaticResource FieldBackgroundBrush}" BorderBrush="{StaticResource FieldBorderBrush}" Foreground="White" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Image" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" />
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                        <TextBox Text="{Binding ImageSourcePath}" IsReadOnly="True" Width="120" Background="{StaticResource FieldBackgroundBrush}" Foreground="White" />
                        <Button Content="Parcourir" Width="60" Height="28" Margin="8,0,0,0" Click="Browse_Click" />
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="Annuler" Width="96" Margin="12,0,8,0" Command="{Binding CancelCommand}" Style="{DynamicResource SecondaryDialogButton}" />
                    <Button Content="Enregistrer" Width="120" Command="{Binding SaveCommand}" Style="{DynamicResource PrimaryDialogButton}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
