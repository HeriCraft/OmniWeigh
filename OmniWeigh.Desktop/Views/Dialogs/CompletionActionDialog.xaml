<Window x:Class="OmniWeigh.Desktop.Views.Dialogs.CompletionActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terminer la série" Height="300" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource ApplicationBackgroundBrush}"
        WindowStyle="None" AllowsTransparency="True">
    <Border BorderBrush="{StaticResource AccentInfoBrush}" BorderThickness="2" CornerRadius="8" Background="{StaticResource CardBackgroundBrush}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="SÉRIE TERMINÉE" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Grid.Row="1" Text="Sélectionnez le format de document et l'action à effectuer :" Foreground="{StaticResource MutedTextBrush}" FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                <TextBlock Text="Format du document" Foreground="#6C829D" FontSize="11" Margin="0,0,0,5"/>
                <ComboBox x:Name="FormatComboBox" Height="35" Width="250" SelectedIndex="0">
                    <ComboBoxItem Content="Format A4 (Standard)"/>
                    <ComboBoxItem Content="Format 80x80 (Imprimante Thermique)"/>
                </ComboBox>
            </StackPanel>

            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Annuler" Click="BtnCancel_Click" Background="{StaticResource ResetButtonBrush}" Foreground="White" Height="40" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                <Button Grid.Column="2" Content="🖨️ Imprimer" Click="BtnPrint_Click" Background="{StaticResource PrintButtonBrush}" Foreground="White" FontWeight="Bold" Height="40" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                <Button Grid.Column="4" Content="💾 PDF" Click="BtnPdf_Click" Background="{StaticResource SaveButtonBrush}" Foreground="White" FontWeight="Bold" Height="40" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
            </Grid>
        </Grid>
    </Border>
</Window>
